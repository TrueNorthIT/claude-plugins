# <Confirmation heading — sentence case. Examples: "We've got your report", "Your application is in">

<Reference line — usually the platform renders this automatically from `confirmation.summary` in service.json. Use this page only if you need extra static content below the auto-rendered panel.>

## What happens next

<One short paragraph. Active voice. No "please". Tell the user concretely what comes next and when.>

## If you need to change something

If you want to update your answers, [get in touch](/contact) and quote your reference number.

<!--
Notes for the author (delete before saving):
- The auto-rendered confirmation panel is driven by `confirmation` in service.json. This file is only needed if you have static content to add below it (a "what happens next" section, an offline next-step explanation, signposting to related services).
- If the journey's confirmation block in service.json already covers everything you want to say, do NOT create this file — leave `pages` ending on the last question page.
- Do not include a "Submit again" or "Start over" button here. Those decisions belong to the citizen via the back button or the homepage.
-->
