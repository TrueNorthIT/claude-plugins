{
  "name": "truenorthit",
  "owner": {
    "name": "TrueNorth IT",
    "email": "hello@truenorthit.co.uk"
  },
  "description": "Claude Code plugins for building against TrueNorth IT services — the Dataverse Contact API, GOV.UK Design System services, and local model offload.",
  "plugins": [
    {
      "name": "dataverse-contact-api",
      "source": "./plugins/dataverse-contact-api",
      "description": "How the Dataverse Contact API works — the me/team/all/public tiers, the permission grammar, the non-OData query dialect, Entra External ID auth, the SDK, and how to triage a 401/403/404/empty list. The shared reference behind dataverse-portal and dataverse-terraform."
    },
    {
      "name": "dataverse-portal",
      "source": "./plugins/dataverse-portal",
      "description": "Scaffold a React + TypeScript + Tailwind SPA that signs citizens in with Microsoft Entra External ID (MSAL) and consumes the Dataverse Contact API, following the patterns from dataverse-example-case-portal."
    },
    {
      "name": "create-gds-service",
      "source": "./plugins/create-gds-service",
      "description": "Scaffold or remove a GOV.UK Design System (GDS) service in a service-builder-flow style repo — folder, pages, GDS-compliant copy, Dataverse row CLI (with safety-gated delete), and OpenAPI verification."
    },
    {
      "name": "dataverse-terraform",
      "source": "./plugins/dataverse-terraform",
      "description": "Define, and explain, a Dataverse Contact API portal backend as Terraform — export an existing scope into HCL and adopt it into state, scaffold and provision a new one, or read a config back in plain English. Uses the public TrueNorthIT/dataversecontact provider."
    },
    {
      "name": "bigmac",
      "source": "./plugins/bigmac",
      "description": "Offload grunt work (summaries, first-pass code exploration, bulk classification, drafts) to bigmac, the office Ollama server, instead of burning frontier tokens. Never touches Claude Code's own endpoint or login."
    }
  ]
}
